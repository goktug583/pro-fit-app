<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>ASTRUM // BODY ANALYTICS</title>
<!-- Minimalist ve Fütüristik Font -->
<link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@200;400;600&family=Montserrat:wght@100;300;500&display=swap" rel="stylesheet">
<style>
    :root {
        --space-black: #050508;
        --deep-blue: #0b1019;
        --star-white: #ffffff;
        --starlight: rgba(255, 255, 255, 0.8);
        --accent-blue: #a2d2ff;
        --accent-purple: #b8c0ff;
        --glass-bg: rgba(255, 255, 255, 0.03);
        --glass-border: rgba(255, 255, 255, 0.1);
        --input-bg: rgba(0, 0, 0, 0.3);
    }

    * { box-sizing: border-box; }

    body {
        margin: 0;
        padding: 0;
        font-family: 'Montserrat', sans-serif;
        background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
        color: var(--star-white);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow-x: hidden;
    }

    /* Arka Plan Yıldızları (CSS ile) */
    body::before {
        content: "";
        position: fixed;
        top: 0; left: 0; width: 100%; height: 100%;
        background-image: 
            radial-gradient(white 1px, transparent 1px),
            radial-gradient(white 1px, transparent 1px);
        background-size: 50px 50px, 100px 100px;
        background-position: 0 0, 25px 25px;
        opacity: 0.1;
        z-index: -1;
    }

    .wrapper {
        width: 100%;
        max-width: 1100px;
        padding: 40px;
        z-index: 2;
    }

    .galaxy-card {
        background: var(--glass-bg);
        backdrop-filter: blur(20px); /* Buzlu Cam Etkisi */
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--glass-border);
        border-radius: 30px;
        padding: 50px;
        box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        position: relative;
        overflow: hidden;
    }

    /* Kartın üstündeki parlama efekti */
    .galaxy-card::after {
        content: "";
        position: absolute;
        top: -50%; left: -50%; width: 200%; height: 200%;
        background: radial-gradient(circle, rgba(162,210,255,0.05) 0%, transparent 60%);
        pointer-events: none;
    }

    h1.title {
        font-family: 'Exo 2', sans-serif;
        font-weight: 200;
        font-size: 42px;
        margin: 0;
        letter-spacing: 8px;
        text-transform: uppercase;
        background: linear-gradient(to right, #fff, #a2d2ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .subtitle {
        font-size: 10px;
        letter-spacing: 4px;
        color: var(--accent-blue);
        margin-bottom: 40px;
        text-transform: uppercase;
        opacity: 0.7;
    }

    .grid-layout {
        display: grid;
        grid-template-columns: 1fr 1.2fr;
        gap: 60px;
    }

    @media(max-width: 850px) {
        .grid-layout { grid-template-columns: 1fr; gap: 40px; }
    }

    /* FORM STİLLERİ - MİNİMALİST */
    .input-row {
        display: flex;
        gap: 20px;
        margin-bottom: 20px;
    }
    
    .input-group {
        flex: 1;
        position: relative;
    }

    label {
        font-size: 10px;
        letter-spacing: 1px;
        color: var(--accent-blue);
        margin-bottom: 8px;
        display: block;
        text-transform: uppercase;
    }

    input, select {
        width: 100%;
        background: var(--input-bg);
        border: none;
        border-bottom: 1px solid var(--glass-border);
        color: #fff;
        padding: 15px 0;
        font-family: 'Exo 2', sans-serif;
        font-size: 16px;
        font-weight: 300;
        transition: 0.4s;
        border-radius: 0; /* Keskin kenarlar */
    }

    input:focus, select:focus {
        outline: none;
        border-bottom-color: var(--accent-blue);
        background: linear-gradient(to top, rgba(162,210,255,0.05), transparent);
    }

    /* Placeholder rengi */
    ::placeholder { color: rgba(255,255,255,0.15); }

    .galaxy-btn {
        width: 100%;
        padding: 18px;
        margin-top: 30px;
        background: rgba(255,255,255,0.05);
        border: 1px solid var(--glass-border);
        color: var(--accent-blue);
        font-family: 'Exo 2', sans-serif;
        font-size: 14px;
        letter-spacing: 4px;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.5s ease;
        border-radius: 50px;
    }

    .galaxy-btn:hover {
        background: var(--accent-blue);
        color: var(--space-black);
        box-shadow: 0 0 30px var(--accent-blue);
        letter-spacing: 6px;
    }

    /* SONUÇ PANELİ */
    .results-container {
        border-left: 1px solid var(--glass-border);
        padding-left: 60px;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    @media(max-width: 850px) {
        .results-container { border-left: none; border-top: 1px solid var(--glass-border); padding-left: 0; padding-top: 40px; }
    }

    .stat-big {
        margin-bottom: 30px;
    }

    .stat-label {
        font-size: 10px;
        letter-spacing: 2px;
        color: rgba(255,255,255,0.4);
        display: block;
        margin-bottom: 5px;
    }

    .stat-value-main {
        font-family: 'Exo 2', sans-serif;
        font-size: 64px;
        font-weight: 100;
        background: linear-gradient(to bottom, #fff, #aaa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        line-height: 1;
    }

    .stat-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 25px;
        margin-bottom: 30px;
    }

    .stat-small-value {
        font-size: 24px;
        font-weight: 300;
        color: var(--accent-blue);
    }

    .assessment-box {
        margin-top: 20px;
        padding-top: 20px;
        border-top: 1px solid var(--glass-border);
    }

    .verdict {
        font-size: 18px;
        font-weight: 300;
        color: #fff;
        margin-bottom: 5px;
    }

    .verdict-detail {
        font-size: 12px;
        color: rgba(255,255,255,0.5);
        font-weight: 300;
        line-height: 1.6;
    }

    /* Boş Durum */
    .empty-state {
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        opacity: 0.2;
    }
    
    .orbit {
        width: 100px; height: 100px;
        border: 1px solid #fff;
        border-radius: 50%;
        animation: spin 10s linear infinite;
    }
    
    @keyframes spin { 100% { transform: rotate(360deg); } }

    .error-msg {
        color: #ff6b6b;
        font-size: 12px;
        margin-bottom: 20px;
        letter-spacing: 1px;
    }

</style>
</head>
<body>

<div class="wrapper">
    <div class="galaxy-card">
        <h1 class="title">ASTRUM</h1>
        <div class="subtitle">Advanced Body Analytics System</div>

        <div class="grid-layout">
            <!-- GİRDİ ALANI -->
            <div>
                {% if error_message %}
                    <div class="error-msg">:: SİSTEM HATASI :: {{ error_message }}</div>
                {% endif %}

                <form method="POST" action="/calculate">
                    <div class="input-row">
                        <div class="input-group">
                            <label>Kilo (kg)</label>
                            <input name="weight" type="number" step="0.1" required placeholder="86">
                        </div>
                        <div class="input-group">
                            <label>Boy (m)</label>
                            <input name="height" type="number" step="0.01" required placeholder="1.83">
                        </div>
                    </div>

                    <div class="input-group" style="margin-bottom:20px;">
                        <label>Yaş</label>
                        <input name="age" type="number" required placeholder="22">
                    </div>

                    <div class="input-row">
                        <div class="input-group">
                            <label>Boyun (cm)</label>
                            <input name="neck" type="number" step="0.5" placeholder="Opsiyonel">
                        </div>
                        <div class="input-group">
                            <label>Bel (cm)</label>
                            <input name="waist" type="number" step="0.5" placeholder="Opsiyonel">
                        </div>
                    </div>

                    <div class="input-group" style="margin-bottom:20px;">
                        <label>Cinsiyet</label>
                        <select name="gender">
                            <option value="male">Erkek</option>
                            <option value="female">Kadın</option>
                        </select>
                    </div>

                    <div class="input-group" style="margin-bottom:20px;">
                        <label>Aktivite Seviyesi</label>
                        <select name="activity">
                            <option value="sedentary">Sedanter (Masa Başı)</option>
                            <option value="light">Hafif Aktif</option>
                            <option value="moderate">Orta Aktif</option>
                            <option value="active">Yüksek Aktif</option>
                            <option value="very_active">Atletik</option>
                        </select>
                    </div>

                    <div class="input-group">
                        <label>Hedef</label>
                        <select name="goal">
                            <option value="lose">Yağ Kaybı</option>
                            <option value="gain">Kas Kazanımı</option>
                            <option value="recomp">Vücut Kompozisyonu (Recomp)</option>
                            <option value="strength">Güç Odaklı</option>
                        </select>
                    </div>

                    <button type="submit" class="galaxy-btn">Verileri İşle</button>
                </form>
            </div>

            <!-- SONUÇ ALANI -->
            <div class="results-container">
                {% if bmi is defined %}
                    <div class="stat-big">
                        <span class="stat-label">VÜCUT KİTLE İNDEKSİ (BMI)</span>
                        <div class="stat-value-main">{{ bmi }}</div>
                        <div style="color: var(--accent-blue); font-size: 12px; letter-spacing: 2px; margin-top: 5px;">{{ bmi_risk }}</div>
                    </div>

                    <div class="stat-grid">
                        <div>
                            <span class="stat-label">GÜNLÜK ENERJİ (TDEE)</span>
                            <div class="stat-small-value">{{ tdee }}</div>
                        </div>
                        <div>
                            <span class="stat-label">HEDEF KALORİ</span>
                            <div class="stat-small-value" style="color:#fff;">{{ cal_goal }}</div>
                        </div>
                    </div>

                    <div class="stat-grid">
                        <div>
                            <span class="stat-label">PROTEİN</span>
                            <div class="stat-small-value">{{ protein }}g</div>
                        </div>
                        <div>
                            <span class="stat-label">YAĞ ORANI</span>
                            <div class="stat-small-value">{{ fat_pct }}%</div>
                        </div>
                    </div>

                    <div class="assessment-box">
                        <span class="stat-label">SİSTEM ANALİZİ</span>
                        <div class="verdict">{{ overall_assessment }}</div>
                        
                        <div style="margin-top: 15px; display: flex; gap: 10px; font-size: 10px; color: rgba(255,255,255,0.3);">
                            <div>Y: {{ fat }}g</div>
                            <div>K: {{ carbs }}g</div>
                            <div>BMR: {{ bmr }}</div>
                        </div>
                    </div>

                {% else %}
                    <div class="empty-state">
                        <div class="orbit"></div>
                        <div style="margin-top: 20px; letter-spacing: 2px; font-size: 10px;">BEKLENİYOR</div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

</body>
</html>

-------------------------------------------------

